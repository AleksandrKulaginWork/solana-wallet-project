// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/solana-wallet-project",
    output: "export",
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;